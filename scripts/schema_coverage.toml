# Schema-coverage manifest for scripts/schema_diff.py --check.
#
# Each [[pair]] is an (ooxmlsdk CT_* struct, xlcore-types DTO) we have opened up.
# Per field the diff assigns: covered | flattened (n/m) | derived | excluded |
# MISSING. Only MISSING counts as a gap; --check fails (non-zero) on any MISSING
# field that is not declared here or via a DTO `schema-excluded:` doc annotation.
#
#   ns       namespace prefix to disambiguate same-named structs (c/x/a)
#   derived  fields the API computes internally (axis ids, refs, order, index)
#   excluded fields intentionally deferred for this pair (extLst is intrinsic)
#   aliases  sdk leaf-tag/field -> dto field, for semantic renames
#
# Deferrals shared by a DTO live in its doc comment (`schema-excluded:`); per-pair
# specifics live below. Every entry is a deliberate decision.
#
# `schema_diff.py --audit` walks every chart-namespace container struct reachable
# from c:chart and requires each to be either a [[pair]] above or a [[deferred]]
# acknowledgement below (with a note). This turns "we forgot to model X" into a
# CI failure instead of relying on memory. `--check` runs both pairs and audit.

# --- chart axes -------------------------------------------------------------
[[pair]]
sdk = "ValueAxis"
dto = "ChartAxisPatch"
ns = "c"
derived = ["axId", "axPos", "crossAx"]
aliases = { delete = "hidden", numFmt = "number_format", orientation = "reversed", txPr = "label_rotation" }

[[pair]]
sdk = "CategoryAxis"
dto = "ChartAxisPatch"
ns = "c"
derived = ["axId", "axPos", "crossAx"]
aliases = { delete = "hidden", numFmt = "number_format", orientation = "reversed", txPr = "label_rotation" }

# --- chart series -----------------------------------------------------------
[[pair]]
sdk = "BarChartSeries"
dto = "ChartSeriesPatch"
ns = "c"
derived = ["idx", "order", "tx", "cat", "val"]
aliases = { dPt = "data_points" }

[[pair]]
sdk = "LineChartSeries"
dto = "ChartSeriesPatch"
ns = "c"
derived = ["idx", "order", "tx", "cat", "val"]
aliases = { dPt = "data_points" }

[[pair]]
sdk = "PieChartSeries"
dto = "ChartSeriesPatch"
ns = "c"
derived = ["idx", "order", "tx", "cat", "val"]
aliases = { dPt = "data_points" }

[[pair]]
sdk = "ScatterChartSeries"
dto = "ChartSeriesPatch"
ns = "c"
derived = ["idx", "order", "tx", "xVal", "yVal"]
aliases = { dPt = "data_points" }

[[pair]]
sdk = "DataPoint"
dto = "ChartDataPoint"
ns = "c"
aliases = { spPr = "fill" }

[[pair]]
sdk = "Marker"
dto = "ChartMarker"
ns = "c"
aliases = { symbol = "style" }

[[pair]]
sdk = "GradientFill"
dto = "ChartGradientFill"
ns = "a"
excluded = ["flip", "rotWithShape", "tileRect"]
aliases = { gsLst = "stops", lin = "angle" }

[[pair]]
sdk = "PatternFill"
dto = "ChartPatternFill"
ns = "a"
aliases = { prst = "preset", fgClr = "foreground", bgClr = "background" }

[[pair]]
sdk = "DataLabel"
dto = "ChartDataLabel"
ns = "c"
derived = ["index"]
aliases = { dLblPos = "position", numFmt = "number_format" }

[[pair]]
sdk = "Trendline"
dto = "ChartTrendline"
ns = "c"
aliases = { trendlineType = "kind", dispRSqr = "display_r_squared" }

[[pair]]
sdk = "ErrorBars"
dto = "ChartErrorBars"
ns = "c"
aliases = { errDir = "direction", errBarType = "bar_type", errValType = "value_type", val = "value", plus = "plus_ref", minus = "minus_ref" }

[[pair]]
sdk = "DataTable"
dto = "ChartDataTable"
ns = "c"
aliases = { showHorzBorder = "show_horizontal_border", showVertBorder = "show_vertical_border", showOutline = "show_outline" }

# --- chart kinds (plot-group containers) ------------------------------------
[[pair]]
sdk = "DoughnutChart"
dto = "ChartPatch"
ns = "c"
aliases = { ser = "series" }

[[pair]]
sdk = "StockChart"
dto = "ChartPatch"
ns = "c"
derived = ["axId"]
aliases = { ser = "series" }

# --- 3D chart kinds ---------------------------------------------------------
[[pair]]
sdk = "View3D"
dto = "ChartView3D"
ns = "c"
aliases = { rotX = "rot_x", rotY = "rot_y", hPercent = "height_percent" }

[[pair]]
sdk = "Bar3DChart"
dto = "ChartPatch"
ns = "c"
derived = ["barDir", "axId"]
aliases = { ser = "series", grouping = "stacking", shape = "bar_shape", gapDepth = "gap_depth" }

[[pair]]
sdk = "Line3DChart"
dto = "ChartPatch"
ns = "c"
derived = ["axId"]
excluded = ["gapDepth"]
aliases = { ser = "series", grouping = "stacking" }

[[pair]]
sdk = "Area3DChart"
dto = "ChartPatch"
ns = "c"
derived = ["axId"]
excluded = ["gapDepth"]
aliases = { ser = "series", grouping = "stacking" }

[[pair]]
sdk = "Pie3DChart"
dto = "ChartPatch"
ns = "c"
aliases = { ser = "series" }

# --- surface chart kinds ----------------------------------------------------
[[pair]]
sdk = "SurfaceChart"
dto = "ChartPatch"
ns = "c"
derived = ["axId"]
excluded = ["bandFmts"]
aliases = { ser = "series" }

[[pair]]
sdk = "Surface3DChart"
dto = "ChartPatch"
ns = "c"
derived = ["axId"]
excluded = ["bandFmts"]
aliases = { ser = "series" }

# --- 3D floor / walls -------------------------------------------------------
[[pair]]
sdk = "Floor"
dto = "ChartSurfaceWall"
ns = "c"
excluded = ["thickness", "pictureOptions"]
aliases = { spPr = "fill" }

[[pair]]
sdk = "SideWall"
dto = "ChartSurfaceWall"
ns = "c"
excluded = ["thickness", "pictureOptions"]
aliases = { spPr = "fill" }

[[pair]]
sdk = "BackWall"
dto = "ChartSurfaceWall"
ns = "c"
excluded = ["thickness", "pictureOptions"]
aliases = { spPr = "fill" }

# --- ofPie chart kinds ------------------------------------------------------
[[pair]]
sdk = "OfPieChart"
dto = "ChartPatch"
ns = "c"
derived = ["ofPieType"]
excluded = ["custSplit"]
aliases = { ser = "series" }

# --- plot-area / legend styling ---------------------------------------------
[[pair]]
sdk = "PlotArea"
dto = "ChartPlotArea"
ns = "c"
derived = ["areaChart", "area3DChart", "lineChart", "line3DChart", "stockChart", "radarChart", "scatterChart", "pieChart", "pie3DChart", "doughnutChart", "barChart", "bar3DChart", "ofPieChart", "surfaceChart", "surface3DChart", "bubbleChart", "valAx", "catAx", "dateAx", "serAx", "dTable"]
aliases = { spPr = "fill" }

[[pair]]
sdk = "Legend"
dto = "ChartLegend"
ns = "c"
derived = ["legendPos", "overlay"]
excluded = ["legendEntry"]
aliases = { spPr = "fill", txPr = "font" }

[[pair]]
sdk = "Title"
dto = "ChartUpdate"
ns = "c"
derived = ["overlay"]
aliases = { tx = "title", layout = "title_layout", txPr = "title_font", spPr = "title_fill" }

[[pair]]
sdk = "ManualLayout"
dto = "ChartManualLayout"
ns = "c"

# --- styles -----------------------------------------------------------------
[[pair]]
sdk = "Protection"
dto = "ProtectionPatch"
ns = "x"

[[pair]]
sdk = "PatternFill"
dto = "FillPatch"
ns = "x"
aliases = { patternType = "pattern", fgColor = "foreground", bgColor = "background" }

[[pair]]
sdk = "GradientFill"
dto = "GradientFillPatch"
ns = "x"
aliases = { type = "kind", stop = "stops" }

[[pair]]
sdk = "Border"
dto = "BorderPatch"
ns = "x"

[[pair]]
sdk = "Alignment"
dto = "AlignmentPatch"
ns = "x"
aliases = { wrapText = "wrap" }

[[pair]]
sdk = "Font"
dto = "FontPatch"
ns = "x"
aliases = { b = "bold", i = "italic", u = "underline", sz = "size" }

# --- rich text runs in cells -------------------------------------------------
[[pair]]
sdk = "Run"
dto = "RichTextRun"
ns = "x"
aliases = { rPr = "font", t = "text" }

[[pair]]
sdk = "InlineString"
dto = "RichText"
ns = "x"
aliases = { r = "runs" }

# --- named cell styles -------------------------------------------------------
[[pair]]
sdk = "CellStyle"
dto = "NamedStylePatch"
ns = "x"
derived = ["xfId"]

# --- non-chart writers (cell-level CRUD parity) -----------------------------
[[pair]]
sdk = "Hyperlink"
dto = "HyperlinkPatch"
ns = "x"
derived = ["reference"]
aliases = { id = "target" }

[[pair]]
sdk = "DataValidation"
dto = "DataValidationPatch"
ns = "x"
derived = ["sequenceOfReferences"]
excluded = ["imeMode", "list"]
aliases = { type = "ruleType" }

[[pair]]
sdk = "ConditionalFormattingRule"
dto = "ConditionalFormatRulePatch"
ns = "x"
derived = ["formatId"]
excluded = ["timePeriod"]
aliases = { type = "kind", formula = "formula1" }

[[pair]]
sdk = "DefinedName"
dto = "DefinedNamePatch"
ns = "x"
derived = ["localSheetId"]
excluded = ["customMenu", "description", "help", "statusBar", "function", "vbProcedure", "xlm", "functionGroupId", "shortcutKey", "publishToServer", "workbookParameter"]
aliases = { xml_content = "reference" }

[[pair]]
sdk = "Table"
dto = "TablePatch"
ns = "x"
derived = ["id", "totalsRowShown"]
excluded = ["comment", "tableType", "insertRow", "insertRowShift", "published", "connectionId", "headerRowDxfId", "dataDxfId", "totalsRowDxfId", "headerRowBorderDxfId", "tableBorderDxfId", "totalsRowBorderDxfId", "headerRowCellStyle", "dataCellStyle", "totalsRowCellStyle"]
aliases = { tableColumns = "columns" }

# --- chart container acknowledgements (schema_diff.py --audit) --------------
# Every chart-namespace container reachable from c:chart that has no [[pair]].
# Most are deliberately flattened into ChartPatch/ChartSeriesPatch/ChartAxisPatch
# fields (the diff can't recurse through a flattened scalar, so we acknowledge
# them here); the rest are genuinely unmodeled. Promote to a [[pair]] when a
# struct gets a dedicated DTO.

# chart-type plot containers (kind/grouping/dir flattened into ChartPatch + series)
[[deferred]]
sdk = "AreaChart"
note = "chart-type container; kind/grouping flattened into ChartPatch + per-series patches"
[[deferred]]
sdk = "BarChart"
note = "chart-type container; kind/barDir/grouping/gapWidth/overlap flattened into ChartPatch"
[[deferred]]
sdk = "BubbleChart"
note = "chart-type container; bubbleScale/sizeRepresents flattened into ChartPatch"
[[deferred]]
sdk = "LineChart"
note = "chart-type container; grouping/marker/seriesLines flattened into ChartPatch"
[[deferred]]
sdk = "PieChart"
note = "chart-type container; firstSliceAngle/holeSize flattened into ChartPatch"
[[deferred]]
sdk = "RadarChart"
note = "chart-type container; radarStyle flattened into ChartPatch"
[[deferred]]
sdk = "ScatterChart"
note = "chart-type container; scatterStyle flattened into ChartPatch"

# series containers (flattened into ChartSeriesPatch / render ChartSeries)
[[deferred]]
sdk = "AreaChartSeries"
note = "series flattened into ChartSeriesPatch"
[[deferred]]
sdk = "BubbleChartSeries"
note = "series flattened into ChartSeriesPatch"
[[deferred]]
sdk = "RadarChartSeries"
note = "series flattened into ChartSeriesPatch"
[[deferred]]
sdk = "SurfaceChartSeries"
note = "series flattened into ChartSeriesPatch"

# series data refs/caches (resolved to values/categories, not authored as structure)
[[deferred]]
sdk = "NumberReference"
note = "numeric data ref; resolved to values"
[[deferred]]
sdk = "StringReference"
note = "string data ref; resolved to categories"
[[deferred]]
sdk = "MultiLevelStringReference"
note = "multi-level category ref; resolved to categories"
[[deferred]]
sdk = "NumberLiteral"
note = "inline numeric literal data; resolved to values"
[[deferred]]
sdk = "StringLiteral"
note = "inline string literal data; resolved to categories"
[[deferred]]
sdk = "NumberingCache"
note = "cached numeric values; read-time data, not authored"
[[deferred]]
sdk = "StringCache"
note = "cached category strings; read-time data, not authored"
[[deferred]]
sdk = "MultiLevelStringCache"
note = "cached multi-level labels; read-time data, not authored"
[[deferred]]
sdk = "Values"
note = "series Y data ref; resolved to values"
[[deferred]]
sdk = "XValues"
note = "scatter/bubble X data ref; resolved to values"
[[deferred]]
sdk = "YValues"
note = "scatter/bubble Y data ref; resolved to values"
[[deferred]]
sdk = "BubbleSize"
note = "bubble size data ref; resolved to values"
[[deferred]]
sdk = "CategoryAxisData"
note = "category data ref; resolved to categories"
[[deferred]]
sdk = "SeriesText"
note = "series name ref; resolved to series name"
[[deferred]]
sdk = "Level"
note = "multi-level category level; resolved via categories"

# axis sub-structures (flattened into ChartAxisPatch)
[[deferred]]
sdk = "Scaling"
note = "min/max/orientation/logBase flattened into ChartAxisPatch"
[[deferred]]
sdk = "MajorGridlines"
note = "flattened into ChartAxisPatch.major_gridlines / showMajorGridlines"
[[deferred]]
sdk = "MinorGridlines"
note = "minor gridlines not modeled"
[[deferred]]
sdk = "DisplayUnits"
note = "flattened into ChartAxisPatch dispUnits"
[[deferred]]
sdk = "DisplayUnitsLabel"
note = "display-units label flattened into ChartAxisPatch"
[[deferred]]
sdk = "DateAxis"
note = "date axis type not modeled (cat/val axes only)"
[[deferred]]
sdk = "SeriesAxis"
note = "series (depth) axis not modeled (cat/val axes only)"

# stock/line decorations (flattened into ChartPatch booleans)
[[deferred]]
sdk = "HighLowLines"
note = "flattened into ChartPatch.hi_low_lines"
[[deferred]]
sdk = "DropLines"
note = "flattened into ChartPatch.drop_lines"
[[deferred]]
sdk = "UpDownBars"
note = "flattened into ChartPatch.up_down_bars"
[[deferred]]
sdk = "UpBars"
note = "up-bars flattened into ChartPatch.up_down_bars"
[[deferred]]
sdk = "DownBars"
note = "down-bars flattened into ChartPatch.up_down_bars"
[[deferred]]
sdk = "SeriesLines"
note = "flattened into ChartPatch.series_lines"

# error bars
[[deferred]]
sdk = "Plus"
note = "error-bar plus value; flattened into ErrorBars pair"
[[deferred]]
sdk = "Minus"
note = "error-bar minus value; flattened into ErrorBars pair"

# labels / text / layout wrappers
[[deferred]]
sdk = "DataLabels"
note = "data-label settings flattened into DataLabels DTO"
[[deferred]]
sdk = "ChartText"
note = "title rich-text/strRef; flattened into title string + titleFont"
[[deferred]]
sdk = "Layout"
note = "c:layout wrapper; manual layout modeled via ManualLayout pair"
[[deferred]]
sdk = "LegendEntry"
note = "per-entry legend visibility/format not modeled"
[[deferred]]
sdk = "TrendlineLabel"
note = "trendline label text/format not modeled (Trendline pair covers the line)"

# of-pie / pivot / picture / surface extras
[[deferred]]
sdk = "CustomSplit"
note = "of-pie custom split not modeled (splitType/splitPos only)"
[[deferred]]
sdk = "PivotFormat"
note = "pivot-chart per-element format not modeled"
[[deferred]]
sdk = "PivotFormats"
note = "pivot-chart formats not modeled"
[[deferred]]
sdk = "PictureOptions"
note = "picture-fill options not modeled"
[[deferred]]
sdk = "BandFormat"
note = "surface band format not modeled"
[[deferred]]
sdk = "BandFormats"
note = "surface band formats not modeled"

# audit root
[[deferred]]
sdk = "Chart"
note = "audit root; children fanned out across ChartPatch/ChartUpdate"
